# RUMAD Photo Uploader — Mobile (Expo)

This folder is the Expo project for the workshop.

## Run the app

From the repo root:

```bash
cd mobile
npm install
npx expo start
```

Develop in the `app/` directory (this project uses Expo Router).

More Expo docs: https://docs.expo.dev/
